import 'package:flutter/material.dart';

class AppColors {
  AppColors._(); // Clase estática (no instanciable)

  // Colores de la paleta (basados en la imagen compartida)
  static const Color primary = Color(0xFF111827);

  static const Color secondary = Color(0xffffffff);

  static const Color highlight = Color(0xff1F2937);

  static const Color borderColor = Color(0xff4B5563);

  static const Color primaryText = Color(0xffffffff);

  static const Color secondaryText = Color(0xffffffff);

  static const Color captionText = Color(0xffffffff);

  static const Color inputTextBlack = Color(0xff000000);

  static const Color placeholderText = Color(0xffAEB8C9);

  static const Color backgroundColor = Color(0xff000000);

  static const Color danger =
      Color(0xFFFF4B4B); // Rojo para acciones peligrosas

  static const Color blue = Color(0xFF00CFFF); // Azul para plan Elite

  static const Color gold = Color(0xFFF59E0B); // Gold Figma
  static const Color elite = Color(0xFF1FD0EB); // Elite Figma

  static const Color disabled =
      Color(0xFF4B5563); // Color para estado deshabilitado
}
